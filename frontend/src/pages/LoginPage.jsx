import React from 'react';

// ======================================================
// LOGIN PAGE
// Renders authentication entry point
// Delegates all logic to AuthContext
// ======================================================

const LoginPage = () => {
  return (
    <div className="page login">
      <h1>Welcome to Aura OS</h1>
      <p>Authentication system — Phase 2 implementation.</p>
    </div>
  );
};

export default LoginPage;
